# text2ontology 后端配置模板
#
# 用法：复制为 .env 再按需修改。
#     cp .env.example .env
#
# 优先级（高 → 低）：
#   1. 命令行 flag         （-dsn / -port / -static）
#   2. shell / systemd 环境变量
#   3. 本文件（.env / ../.env / ../../.env）
#   4. 代码里硬编码的默认值
#
# 注意：.env 已列入 .gitignore，不会被提交。这份 .env.example 会被提交。
#      用于生产的真实凭据**不要**写进 .env.example。

# ===== 数据库连接 =====
# 留空或删除此行 → 回落到代码默认值（本机 docker-compose 的 postgres）。
# host 用 localhost 作示例；真实部署请换成 env 注入，不要把具体主机名写进仓库。
#
# 开发者注意：请把下面的 <CHANGE_ME> 改为你自己的 Postgres 密码，
#           建议用 `openssl rand -base64 24` 生成强随机口令，并同步把
#           lakehouse2ontology-enterprise 角色的密码改为相同值。
#
# 注意：username == password 是弱口令，首次 clone 后请务必换密码，
#      生产环境绝对不能保留这种默认值。
DATABASE_URL=postgres://lakehouse2ontology-enterprise:<CHANGE_ME>@localhost:5438/lakehouse2ontology-enterprise?sslmode=disable

# ===== HTTP 服务 =====
# 本仓库服务端口限定 18000-19000 段（:8090 保留给 sibling text2dax-ontology pristine monolith）。
PORT=18091
STATIC_DIR=../frontend/out
